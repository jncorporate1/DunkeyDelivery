
@{
    ViewBag.Title = "ManageUsers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="right_col" role="main">

    <div class="row">
        <div class="col-md-12 col-xs-12">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success" id="success-alert">
                    <button type="button" class="close" data-dismiss="alert">x</button>
                    <strong></strong><span> @TempData["SuccessMessage"]</span>
                </div>
            }

            <div class="panel-heading">
                <div class="x_title">
                    <div class="row">
                        <h3 class="pull-left">Manage Admins</h3>
                        <div class="pull-right">
                            <a href="@Url.Action("Index","Admin")" class="btn bg-blue">
                                <i class="fa fa-plus-square"></i>
                                Add new
                            </a>
                        </div>
                    </div>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    @{
                        Html.RenderAction("SearchAdmin", "Admin");
                    }
                    <div id="tblSearchResult">
                        @{
                            Html.RenderAction("SearchAdminResults", "Admin");
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        setTimeout(
  function () {
      if ($('#success-alert') != null) {
          $('#success-alert').slideUp(500, function () {
          });
      }
  }, 3000);

        //var message = $("#hdnMessage").val();
        //if (message != "") {
        //    $("#success-alert span").text(message);
        //    $("#success-alert").fadeTo(2000, 500).slideUp(500, function () {
        //        $("#success-alert").slideUp(500);
        //    });
        //}
    });
</script>

